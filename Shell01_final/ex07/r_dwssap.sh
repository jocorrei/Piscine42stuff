grep -v "#" /etc/passwd | sed "1d" | sed -n "p;n" | sed 's/:.*//' |  rev | sort -r | awk 'NR >= ENVIRON["FT_LINE1"] && NR <= ENVIRON["FT_LINE2"]' | tr '\n' ' ' | sed 's/ /, /g' | sed 's/\(.*\), /\1./' | tr -d '\n'
